export enum LengthUnit {
    KILOMETERS = "km",
    METERS = "m",
    CENTIMETERS = "cm",
    MILLIMETERS = "mm"
}

export enum TimeUnit {
    MILLISECONDS = "ms",
    SECONDS = "s",
    MINUTES = "min",
    HOURS = "h",
    DAYS = "d"
}

export enum VelocityUnit {
    KM_PER_HOUR = "km/h",
    M_PER_SECOND = "m/s"
}