export enum LengthUnit {
    KILOMETERS = "km",
    METERS = "m",
    CENTIMETERS = "cm",
    MILLIMETERS = "mm"
}

export enum TimeUnit {
    MILLISECONDS = "ms",
    SECONDS = "s",
    MINUTES = "min",
    HOURS = "h",
    DAYS = "d"
}